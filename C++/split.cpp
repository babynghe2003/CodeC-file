#include <bits/stdc++.h> 
using namespace std;  
   
int main()  
{  
    string S="a", T;  // declare string variables  
    while (S!=""){
        cin >> S;
        cout << S << endl;
    }
    // getline(cin, S); // use getline() function to read a line of string and store into S variable.  
    // //cout << S;
    //  stringstream X(S); // X is an object of stringstream that references the S string  
    // while (getline(X, T, ' ')) {  
    //      cout << T << endl; 
    //  }  
    system("pause");
    return 0;  
}   